// Free-form gender choices shared by the Add-client form and the Lead form. Stored and
// sent as the plain label ("" when unset) to match the backend's free-form string field.
export const GENDER_OPTIONS = ["Male", "Female", "Other"] as const;
